# watermark-go 长期约束

## 权威性

本文件是 watermark-go 新项目唯一权威长期约束。设计说明、实现计划、代码注释、部署手册或历史项目资料与本文件冲突时，以本文件为准。改变这里的边界必须取得用户明确批准，并在同一变更中更新约束、测试和需求追踪证据。

## 系统职责边界

- Go module language 固定为 `1.24.0`，首选构建工具链固定为 `go1.26.5`；Docker builder 必须精确固定为 `golang:1.26.5`，不得使用浮动 minor 标签。
- Go 服务负责全部 HTTP 协议、业务编排、数据访问和任务调度。主业务不得依赖另一套 watermark 后端。
- 容器内允许受控调用 `yt-dlp`、`ffmpeg` 和 Python bridge。受控调用必须固定依赖版本，限制参数、超时、并发、输入输出与资源，并由 Go 进程管理生命周期；Python bridge 只能是解析适配器，不能成为独立业务服务。
- production parser 必须通过 metadata-driven descriptor registry 注册稳定 key、aliases、精确 domains、
  capabilities、priority 和 query allowlist；重复/歧义 metadata 启动失败。key 必须是稳定 ASCII ID，
  parser constructor 零 I/O，只接收注入依赖，不得在 init/import/构造时联网、读环境、创建目录或启动进程。
  URL canonicalizer 不得联网，任何目标在首次请求前及每次 DNS/dial/redirect 都必须经过 netguard，禁止
  先请求后判断 host。
- 内部富媒体使用强类型 `ImageAsset`/`MediaCandidate`，一次 Parse 最多抓取一次上游快照，候选按明确
  metadata 稳定排序且 fallback 有界。对外保留当前 `images` 字符串数组和既有 aliases，Live Photo 只以
  可选加法 `imageAssets` 投影；任何媒体 URL 都必须通过 SSRF、协议、数量、大小与类型门禁。
- URL 必须分型为不可日志化 `FetchURL`、可持久化 `SafeURL` 和绑定 platform/parser/schema version 的
  不可逆 `CacheKey`；禁止盲改 HTTP→HTTPS、关闭 TLS 或跨 origin/host 转发 Cookie/Authorization/平台
  会话 header。取消、internal、credential_required、schema_changed 和安全拒绝不得进入普通负缓存。
- 运行形态仅允许单机 Docker Compose。不得引入多节点注册、跨节点调度、主从复制、Sentinel、Kubernetes、Swarm 或其他集群控制面。
- 项目不使用 Jenkins，不保留 Jenkins 流水线、凭据或触发逻辑。

## 构建与发布边界

- GitHub Actions 负责测试、安全扫描、构建应用镜像并推送至 GHCR。运行身份必须由 manifest RepoDigest、provenance attestation subject、OCI source/revision 和完整 40 位 source commit 共同绑定；tag 仅作索引。
- 目标机只允许登录镜像仓库、拉取已发布镜像、执行数据库迁移和启动或回滚本项目 Compose 栈。
- 目标机不得执行应用镜像构建、镜像导出或导入，也不得从工作区复制镜像层替代 GHCR 发布物。
- `deploy/compose.yml` 只能引用预构建镜像，不得出现 `build:` 配置。
- builder/runtime/MySQL/Redis/MariaDB recovery image 全部固定官方 registry digest。ffmpeg 必须固定精确包版本与 reproducible snapshot，或固定经审查制品 SHA-256；每个 Python 依赖必须带 hash 并以 `--require-hashes` 安装。
- Go binary 固定使用 `-trimpath -buildvcs=false` 与空 buildid，禁止注入 commit/time；Python 禁止 `.pyc` 并归一化可变 rootfs 元数据。两个不同 revision 构建相同输入时 canonical rootfs/app hash 必须一致，revision 身份只在 OCI config label/attestation。
- CI 普通 push 只运行 verifier unit tests 与 `--schema-of-present`；只有 final evidence push 运行 `--require-complete`。docs/artifacts-only push 不得构建应用镜像或移动 `latest`。
- 发布必须采用两个不同不可变角色：A 完整验收后才成为 recovery digest，B 是后续 promotion 产生的 final digest，且 `recoveryDigest != finalDigest`。A→B source diff allowlist 只允许精确 `release/promotion-marker.txt`/OCI revision；禁止 Go、依赖、Dockerfile、执行输入、config、migration 或 schema 变化。marker 必须被 `.dockerignore` 排除且不得 COPY 进 rootfs。
- A/B 的 rootfs、app binary、tool versions 与 schema 除 OCI label 白名单差异外必须一致。发布、SBOM、扫描与 `repository-and-image.txt` 证据按 A/B role 分开；B attestation/equivalence/`final-shadow-e2e.json` 全部通过后才能原子更新 canonical verified `image-digest.txt`，Task 18 只读该 B digest。

## 当前部署事实与状态机

- 每次部署先重新 discovery，不得把本段记录当作免检缓存。当前事实是 `oldServicePresent=false`：没有运行或停止的 watermark 容器、进程、systemd unit 或本地应用镜像，5001/15001 空闲，`watermark.bxsn.cn` 基线为 502；事实改变必须 fail closed 并重新选择分支。
- 当前源库是 MariaDB 11.8.6，能力为 `@@log_bin=0`、`binlog_format=MIXED`、`gtid_strict_mode=0`，所以 actual 分支固定 `chosenMigrationMode=final_full_no_binlog`；不得声称 GTID/delta 可用，也不得用 `updated_at` 伪造增量。
- 当前回滚模式固定 `rollbackMode=absent_two_stage`：先完成 A shadow 隔离全验，再用干净 final DB 完成 A 真实域名、真微信、固定前端矩阵和 `A observation>=1800s`；之后才允许构建并验收 B、执行 B shadow 与 B→A 真实 drill，最后切 B。
- A bootstrap 失败只能 fence/drain A、撤回本次 route、恢复原 502 路由、保留 final DB/outbox 已接受写入并标记 `FAILED`；在 A 验证前不得声称 5 分钟健康回滚或已有 recovery digest。
- B→A 是当前唯一适用真实 rollback，必须在同一兼容 final DB 上于 300 秒内通过 health/data/route/DB identity。initial/legacy 分支必须记录 `not_applicable_no_verified_legacy_service`；隔离 compatibility rehearsal 不计入 rollback/pass。
- 只有未来 discovery 证明 `oldServicePresent=true` 且旧 service/route/writer/DSN identity 全部验证，conditional legacy reverse 才可适用；其目标必须是唯一指定且实际承接回滚生产流量的隔离旧库克隆，checksum 后原子切换旧服务 DSN 到同一克隆并验证身份，禁止用早期备份覆盖切流后新写入。
- 正式路由权威只来自运行 tunnel/dashboard 的脱敏 route identity，或受控凭据下安全 API 查询，再由真实 HTTPS 探测交叉验证。`/etc/cloudflared/config.yml` 不是权威；不得编辑/重建 token tunnel。
- 默认只允许操作 Compose project `watermark-go`。cutover/rollback 额外对象仅限 `host-before 精确 identity/hash allowlist` 中已验证的旧 watermark service/route；identity 不存在或变化即 fail closed，绝不操作无关容器、进程、systemd、镜像、网络、卷或路由。

## 解析基准硬门槛

解析验收必须同时满足以下条件，任何一项缺失或超限均视为失败：

- 使用基准中同一组 93 个启用样本，不得替换失败样本或改变启用集合；
- 固定并发为 3；
- Go native 解析器启用且 fallback 同时启用；
- 绕过成功缓存、失败缓存和历史结果复用，报告明确记录缓存绕过；
- `success >= 62`；
- 批次墙钟时间 `durationMs <= 216000`。
- 每轮 records 必须恰好覆盖 canonical enabled set 的 93 个唯一 sampleKey；成功项必须含 media success 与本轮唯一 parserInvocationId。completed/success/wall-clock 必须从 records 重算；三轮 runId、时间窗与 record-set 独立，不信任 aggregate 或 `passed`。
- `maxObservedConcurrency` 必须从每条 record 的 started/ended 半开区间重算，不得超过 3，并在 93 个可运行样本下实际达到 3；自报 `concurrency=3` 不能替代区间证明。

## 资源与共存停止线

- 宿主机 CPU 或内存使用率持续达到或超过 85% 时，立即停止新栈的压测、发布或切流。
- 宿主机磁盘使用率达到或超过 85% 时，立即停止新栈的下载、基准、发布或切流。
- 同时采集 MemAvailable、swap `si/so`、memory/io PSI、OOM、I/O error 和 inode；静态 swap 占用不单独误停，持续换页/PSI、低 MemAvailable、OOM/I/O 增量或 inode 越线必须停止新重任务或触发适用回退。
- 新栈不得影响任何其他容器的健康、重启次数、端口、网络、卷或性能；发现影响时立即停止并回滚新栈。
- 不得重启 Docker daemon，不得清理、停止或修改其他项目的容器、镜像、网络和卷。任务 17 只写 before，任务 18 的公网观察结束后才写 final after/diff；无关容器变化必须为零。

## 前端与域名

- 当前前端真实域名是 `https://watermark.bxsn.cn`。A recovery 与 B final 都必须通过该域名完成 session、syncParse、asyncSubmit/asyncPoll、cacheRestore、performance、fallbackCreate/fallbackPoll/fallbackDownload、m3u8Create/m3u8Poll/m3u8Download、video、gallery；每项含 requestId/passed。
- 临时端口或局域网地址仅用于影子验收，不能替代真实域名验收，也不能被写成当前前端默认域名。
- 生产 session 必须由已登录 DevTools/真机提供新的一次性 `wx.login` code；readiness code 不得复用，A/B 正式矩阵开始时都要重新获取。空 code、重放 code、fake exchanger 或主机 Node/npm 不能替代。证据不得保存 code/openid/token/session/secret。
- fallback create 保持匿名兼容但受 attempt/limit/SSRF。新 cache shareId 与 parse task ID 是 crypto-random >=128-bit、用途/TTL 绑定的 bearer capability；legacy shareId 只限流只读且不再生成。fallback poll/download 使用签名 URL；m3u8 poll 兼容前端固定 `/api/task/:id`，以随机 >=128-bit ID 作 bearer，最终 file URL 才签名。发送凭据的 route 同时兼容 `token` 与 Bearer，不得要求前端未发送的 query/header。

## 敏感信息与仓库边界

- 密钥、访问令牌、密码、会话材料、平台 Cookie、私钥、生产环境变量和服务器配置禁止进入 Git 历史、镜像构建上下文、测试夹具、日志及报告。
- `.env*`、私钥和证书文件、含密码或服务器配置命名的文档、cache、logs、tmp、数据库或 Redis 数据、下载媒体及 Docker 导出文件必须同时被 `.gitignore` 和 `.dockerignore` 排除。
- 运行秘密只允许来自环境变量或目标机权限受限的本地运行文件；测试只能使用明显无效的占位值。
- 仓库策略测试和秘密扫描是合并前门禁，不能以人工确认代替。
- 策略门禁必须扫描 tracked worktree、全部可达 commits 和所有即将推送的 refs；失败输出不得回显敏感字面量。独立仓库不得恢复旧仓库 remote 或已清理的导入历史。
- 来源基线只能由 `docs/source-provenance.json` 记录的 repo、commit 与 tree 识别；该文件不得包含访问凭据。
- 外部研究 `ucmao/media-parser` 只能由 `docs/research/media-parser-provenance.json` 固定的 repository、
  branch、commit、tree 与 MIT license hash 识别；当前边界为 `codeCopied=false`、
  `baselineAuthority=false`。其 50-domain 目录只作候选覆盖线索，不得自动进入 production registry 或
  93 样本 trust anchor。未来复制任何实质代码/数据前必须先单独更新归属/license 与 policy 门禁。
- 不得从该研究项目吸收首次请求后才校验 domain、关闭 TLS、硬编码会话/反爬材料、记录原始 URL/query、
  宽权限目录、无界下载/缓存或广泛吞异常做法；研究优点只能在本项目现有 netguard、typed error、资源
  门禁和前端契约之内实现。
- 当前根历史仍未完成唯一 root rewrite、GC 与 full scan 时必须保持 pending；不得仅凭 staged tree 宣称 R1 完成。

## 数据与证据

- MariaDB 备份只允许由 `deploy/image-lock.json` 固定官方 digest 的 MariaDB recovery image 在 Compose `migration-tools` profile 中执行。先恢复到隔离 MariaDB clone，再由 typed importer 写入 MySQL 8.4；禁止裸 `docker run`、裸 tag、本地 build 或直接挂载 MariaDB 数据目录到 MySQL。
- actual final 数据 gate 是 table-scoped fence、writer/连接 identity、重复 hash 无 writer 证明、全新 consistent full snapshot、typed import 和逐表 count/checksum。禁止全实例 read lock/read_only 影响其他数据库；任何差异都禁止启动对应 API/listener/worker。
- Compose profiles 必须使 `shadow DB identity != final DB identity`，并使用独立 DB/schema、独立 Redis namespace 和独立 outbox。shadow E2E/admin/benchmark/pending/outbox 不得进入 final；不得用 cleanup 代替隔离。final 只接收 production full 与 scrub 后生产数据，checksum 必须证明无本轮 shadow/A-B acceptance 的 runId/taskId/sentinel/outbox；legacy snapshot 中原有合法历史 platform_runs/测试记录按 source checksum 保留。
- `/var/lib/watermark-go/runtime.env` 是唯一正式仓库外运行文件，权限固定 0600；final bind 只能是 `127.0.0.1:5001`，shadow 只能是 `127.0.0.1:15001`。
- A 与 B 各完成一份真实域名 >=1800 秒观察：恰好 60 个唯一且严格递增的 raw sample，每次请求真实 `https://watermark.bxsn.cn/api/health`；首个约 +30 秒、第 60 个不早于 +1800 秒、间隔 25–35 秒，逐项绑定 image digest 和 health/latency/restart/OOM/I/O/PSI/磁盘/inode；验证器从 raw samples 重算，不信任 60/60 或 P95 aggregate。
- Task 18 切 B 前安装统一 post-cutover failure trap。步骤 3、4、5、6 任一失败时立即 fence/drain 新写并自动调用已演练 B→A；回退不能证明成功时禁止恢复旧路由，A/B 受控只读/隔离并标记 `FAILED`。
- 每次 B 切换生成唯一 `deploymentRunId`/`cutoverAttemptId`，state/public-cutover/B E2E/B observation/final acceptance/running digest/B final up 必须绑定同一 IDs 与当前时间窗；旧 passed artifact 不匹配即拒绝。trap 以 `set -Eeuo pipefail` 覆盖 ERR/EXIT/HUP/INT/TERM，mutation 前原子写 `in_progress`；重启发现未关闭 attempt 必须先 inspect/fence 并按 A→502 或 B→A reconcile。
- `pull-and-up.txt` 是 versioned A/B role event ledger，完整记录 A/B pull、shadow/final up、actual RepoDigest/config/runtime/data identity 与 `localBuild=false/localLoad=false`。B final inspect 通过才原子更新 B up/`running-digest.txt`，失败或回退不得留下假 B 运行证据。
- `shadow-e2e.json`、`public-cutover.json`、`local-verification.md`、scan/status `.txt` 及其他提交证据必须是含 schemaVersion/passed/run/role 的 machine-readable 文件，经同目录 0600 temp、flush/file fsync、原子 rename、directory fsync 写入。失败写当前 run/attempt 的 passed=false tombstone；无法写时由 ID mismatch 拒绝旧 PASS。
- E2E evidence 不得记录 share/task capability 原值、ticket、原始媒体/分享 URL或完整 path/query；只记录 route template、same-origin/HTTPS、状态、字节数/内容 hash、requestId，capability 省略或不可逆 hash。
- tracked `final-acceptance.md` 不得自含当前 governance commit SHA，只记录 `evidenceParentCommit` 与排除自身的 `evidencePayloadTreeSha256`；提交后由 CI `GITHUB_SHA` 或本地 `git rev-parse HEAD` 外部验证 `verifiedEvidenceCommit`、parent/payload 与 docs/artifacts-only diff，禁止自引用或冒充镜像 commit。
- 本地临时测试或诊断报告只写入被忽略的 `reports/`；需要提交的脱敏验收材料只写入 `artifacts/`。`artifacts/` 不进入镜像构建上下文，证据必须按 A/B role 关联 source commit、attestation 与 digest。
