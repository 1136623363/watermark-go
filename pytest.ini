[pytest]
testpaths = tests/e2e
addopts = -ra --strict-markers
markers =
    smoke: fast health and authentication checks
    e2e: end-to-end API flow checks
    fallback: download fallback flow checks
    admin: admin console API checks
    contract: route contract, validation, and abnormal input checks
    slow: optional checks that depend on external tools or upstream networks
